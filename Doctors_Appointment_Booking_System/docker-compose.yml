
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/usr/src/app
    ports:
      - "8001:8000"
    env_file: .env.dev
    depends_on:
      - db

  db:
    image: postgres:15
    env_file: .env.dev
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data/

volumes:
  postgres_data_dev:
